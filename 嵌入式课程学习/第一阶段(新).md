[TOC]

- 晨会：8：30\~9:00，  下午分享：13：30\~ 14：00
- 上课时间（周一\~周五）：9:00\~12:00，2:00\~5:00，6:00~8:30  
- 周六的自习 ：9:00\~11:30，2:00~5:00
- 每天早上需要报告（需要 ppt，进行半个小时左右的报告）



连接：打开我的电脑，在导航栏上输入 `\\msiteacher` 访问资源。

用户名: stu		密码: gxa1101qaz



## 2026-5-13(王海宁老师 —— C)

### 上午

### 下午

- 效率学习

- 有效沟通

- 就业方向（业务 + 技术  以后找工作，主要根据实际的业务进行寻找，即工作面试问的问题是 对应业务（需要自己了解，如金融、云服务等） + 技术栈（八股文））

	- 研发岗
	- 测试岗
	- FAE（芯片行业，类似销售，需要解决产品问题并且推销出去）

- 学习阶段

	- 第一阶段
		- C
		- 数据结构 力扣（线性结构相关） + 洛谷
		- Linux 操作系统的使用 debian
		- 综合案例：RLC 编解码、UTF-8 编码解码
	- 第二阶段 （Linux 应用开发）
		- 文件、cmake
		- 进程、线程、多任务开发、通信
		- 网络编程 
			- 网络原理
			- 组网原理
			- socket 网络通信
		- 数据库
		- 综合案例：小组自定

	C 底层

	- 第三阶段
		- ARM 体系结构（裸机开发 MCU 开发）
			- 体系架构 （控制器  智能汽车需要的技术 freeRTOS）
	- 第四阶段 系统移植 Linux
		- ARM 系统下移植 Linux 系统
	- 第五阶段 Linux 驱动开发

	C++层

	- 第六阶段 
		- C++应用开发 原理
		- Qt 图形界面开发

- 扩展知识阶段

	- 广泛阅读

	- 经典书籍

		

	- 测试理论

	- 数据库优化 ：mysql 优化

	- 量化交易、加密货币、游戏服务器

		

	- 招聘网站 -》 岗位描述、待遇、岗位要求

- 嵌入式开发 

	- cpu 中央处理器 处理指令（二进制规范集合 ——二进制机器码）
	- 高级语言  翻译官(与具体 cpu 有关 具有解析规则 =》ISO 组织规定（理论标准）=》具体规则由具体厂家规定  MSVC  GNU )   机器码
	- 
	- 2^n^

### 今日小记



## 2026-05-14

### 上午

- 操作系统如何运行软件
  - 计算机的结构图（硬件层 、操作系统层、应用层）
  - 软件运行 =》 机器码符合运行 CPU 的指令架构  + 操作系统的支撑
  - 操作系统 ==》 
  	- 提供了一个 CLI（command-line interface）跟用户进行交互   图形界面只是 CLI 的封装(不是必要的)    
  	- 提供了一个查找（字符串 、关键字）可执行程序名 的依据
  	- 提供一个查找路径 通过环境变量的行为，让用户可指定（绝对路径：从根目录开始，相对路径：相对在哪里去找，去 PATH 的值中依次查找）（每运行一个软件时，会读取当前系统的所有环境变量）
  		- Windows 环境变量
  			- PATH：查找路径   提供路径的前缀
  			- TEMP：临时目录（中间文件）
  			- TMP：临时目录（中间文件）
  - windows 是单用户操作系统、Linux 多用户操作系统（同一时间多个用户操作资源）

- 编译器工作流程
  - 编译器的命令 路径前缀（bin 目录） 公布到 PATH 变量中

  - vscode 编辑器   Visual Studio  集成开发工具（编辑器 + 编译器 MSVC）

  - gcc -v 打印全部流程信息

  	- Target（目标）：cpu 版本-操作系统版本-编译器厂家

  	- Configured with（配置）

  	- 预处理 -E   cpp 进行操作：含有#的预处理指令 进行处理

  		- 宏替换 #define 不做语法检测，纯文本替换

  		- 条件编译 #ifdef #ifndef  #else #endif

  			- DEBUG 版本和 release 版本 gcc -D 宏定义 0

  		- 文件包含 #include

  			- 打开文件名，读取里面的内容，展开到当前源码位置处

  			- 文件名 存在 找到（在哪里找）

  				- 默认位置：<> : 只在编译器提供的头文件目录里查找（表示非找自定义接口）

  					“”：不光在编译器提供的头文件目录里查找，还在当前路径下也查找（表示自己定义的接口）

  				- 额外添加

  					- 自动命令控制编译器去哪里找（-I）

  	- 编译 gcc -S
  		cc/cc1.exe 真正的编译器

  		- 将 高级语言 =》 目标 cpu 体系架构（指令）的 汇编语言

  	- 汇编 gcc -c   as 进行操作

  		- 将 汇编 = 映射表 =》 机器码（目标文件、二进制集合）
  		- cpu 可读

  	- 链接 （装配器）  ld 进行操作

  		- 确定了操作数的位置，从而确定了指令中操作数的值或者地址

  		- 将多个文件组装在一起

  		- cpu 可执行

  			

  		- 编译器会将官方写的 main 调用链接在一起

- 指令 = 操作码 + 操作数

	- 操作数：代码中 或者 内存（寻址）

- 工作目录
  - bin：放可执行文件
  - include：放头文件（接口）
  - lib：放源文件（库文件、接口实现）

- 编译器流程：预处理 =》 编译 =》汇编 =》链接

	工程上：编译阶段 =》 链接阶段

	gcc -c -o 

	会更加文件名的后缀名进行编译

- 代码检测：LLVM 的编译器 Clangd 即编译也能提示 vscode 装 clangd 插件

- 编译器解析行为：对 源码文件（文本文件）按行（分号为一行）解析     识别字符串中单词 、符号

	- 每个标点符号  有默认运算规则  C++运算符的重载 程序员来扩展规则
	- 单词   
		- 已知单词（关键字）
		- 扩展单词（变量）

	- 文本文件：二进制   8bit = 1B 按照以恶搞特定符号编码 显示出符号
		按行解析
	- 二进制文件：特定的解码规则 

- 关键字（映射含义表）

	- sizeof :  sizeof a 或者 sizeof(a) 数据类型或变量所占用的内存空间大小  运行前确定（为了避免关键字与关键字 加了()）  返回值的大小 不确定（根据当前系统来的字决定的  %zd 可变大小 根据操作系统来决定大小）
	- typedef 起别名  编译 后期常用于对指针、结构体起外号
	- struct  结构体声明（自定义类型声明）
	- union 共用体声明（自定义类型声明）

- C 语言基本数据类型（本质分配内存空间 以及大小   运行之前就明确大小）

	- char 
	- short 
	- int：在内存上分配一个 4B 字节大小的空间，空间分配的关键字   声明一个变量名 标识内存空间
	- long
	- long long
	- 程序   移植性很差（变量的大小会变）=》引出了  对关键字其别名进行映射（size_t 类型 编译器厂家提供）

- 自定义数据类型

	- 不建议使用奇数类型
	- 累加 （结构体）
		- struct

	- 共享（共用体）
		- union

	- 枚举（内部是普通的整型类型 比较鸡肋 赋值时可以乱写）
		- enum



## 2026-5-15

编译期（生成可执行文件的时间 占用空间、时间，不影响产品效率） =》 运行期（运行可执行文件 影响）

自定义数据类型

1. 结构体

	- 成员赋值	

		- 初始化语法：

		- ```c
			struct stu a3 = {
			  .x2 = 20,
			   ...
			}; /* GNU gcc扩展语法 MSVC没有这种语法 */
			```

		- 

	- 成员对齐 成员类型+成员顺序 会 影响类型大小

		- 由于 cpu 的内存访问效率 一个周期能够读完对应数据
		- 编译器 默认 成员对齐规范
			1. 一个存储体的容量（成员变量中最大的数据类型做为存储元的单位）
			2. 一个存储元  完整的存储一个成员（不出现成员的一部分）
		- 扩展 成员对齐规范（指定编译器对齐方式）—— #pragme pack()

	- 内存起名字（位域）

		- 用于协议(增加可读性)
		- 使用无符号类型

2. 共用体

3. 枚举类型



八进制 缩小 二进制的表示形式

十六进制 为了让 32/64 位操作系统能除尽 



C 语言解码规则：

​	首位是 0 时，进入到 2/8/16 进制解码单元

​		次位是 x 进入 16 进制解码单元

​		次位是 b 进入 2 进制解码单元

​		否则 进入 8 进制解码单元

​	不是 进入 10 进制解码单元



16 进制 单纯表示数据（无负数）

10 进制 表示数字 （有意义）





有无符号数 编解码规则

- signed 有符号编码解码 (启用补码解码规则)
- unsigned 无符号编码解码 （不启用补码，使用全正权值解码规则）
- = ：初始化、赋值（逐位拷贝） =》 等会右边的空间 逐位（一定规则） 拷贝到 等号左边的空间里面
	- 大空间 拷贝 小空间：截断（从低到高的尽量拷贝）
	- 小空间 拷贝 大空间：补齐（根据小空间的操作约束 来补大空间的剩余位置，有符号 补符号位；无符号 补 0）
- 逻辑运算符：会利用运算数的空间约束进行解码
- ~：逐位取反（不看有无符号）
- 

- 常量表达式【(3600 * 24 * 365)UL】，在编译时就计算完成  ul 表示 unsigned long long
- 补码：最高位是负权值，其他位都是正权值 累加求和
	- -2  ： 补码 1110 =>  -2^3^ + 2^2^+2^1^
	- 补码 -> 原码 ： 快速方法
		- 发现：负数的最高 **负权值** +  其余的正权值（全部有效，为 1） = -1
		- 负数的补码： （负数绝对值 - 1）的原码 取反
		- -100 ： 100 -1 = 99 99 的原码：0110 0011  取反为-100 的补码 1001 1100
- 解码器：%d 、%u、%x （%为占位符 后面跟着解码器  同时会开辟空间）
- 无符号与有符号 进行运算（算术运算、逻辑运算） 会转换成 无符号
	- 当内存空间当作数字（有数学含义），就使用 signed
	- 当内存空间当作数据（没有数学含义），就使用 unsigned





数据（让 人 能认识的 符号  =》映射法  ）：ASCII 码（只用了 7 位，128 种字符）

- 字符符号在内存中有 一张 映射表（二进制 与  字符（字符的映射或者矢量图）的映射）     记录的显示字符的几个关键坐标     <==>	编码表

- ASCII 编码表

	- 00H~1FH 控制字符

	- 2FH~7FH 可见字符

		- 30H~39H 字符数字（0~9）   字符数字 - ‘0’ = 数字（有数学意义）
		-   65~90：
		-  大写字母 比 小写字母的优先级更高
		- ‘符号’   助记符   会去映射表取查这个字符，返回对应的值

	- printf

		- %d   补码解码进行解析
		- %c   查 ASCII
		- 。。。

		

- 中文编码（乱码） =》  GBK Unicode utf8 编码

- 标点符号




## 2026-5-18

- **买手机（买双不买奇——通信（4G、6G）、版本（2 代、4 代））**

- 嵌入式关注的点

	1. 编解码
		- 二进制编解码：怎么分解/组装一串二进制序列  
		- 文本编解码（符合编解码）：符合与二进制序列映射关系（映射表）

- 符合编码

	- ASCII（英语系）
	- 非英语系（国标表 —— 各个国家都有自己的规范）
		- 利用 0x80~0xFF 
		- 中文    GB 系列编码  （2 字节表示一个汉字）   GBK
		-  源文件（vscode 的编码格式）——> 内存存储对应二进制数 —— > 运行时（终端的编码格式 —— Windows 默认为国标码）
	- 万国码（unicode 编码表 —— 理论的编码）  等长编码
		- UTF-16： 16bit   默认用 2 个字节表示一个符号   收录 65536 个符号
		- 扩展  （解决 emoji 表情包冲突问题）
			- 2 字节做为一个面，分了 17 个组 分了不适合传输
			- **utf-8** 的编码方式 对 Unicode 进行编码（具体的实现）   
				- 变长编码
				- 英语系国家   -》 1B 表示一个字符
				- 中文 -》3B 表示一个字节
				- 通过 utf-8 转换成 Unicode 编码（内存中存的值） ？
				- 存储格式  头部标识 图
				- **utf-8 的接口实现**
				- **utf-8 推导** 
				- 有规律，能自校验
	- 程序源文件： 文件使用 utf-8 编码（在任何系统上都能够正常显示）
	- 编译时环境：编译器帮助转码（编译工具链提供 iconv 工具 进行转码）生成一个对应当前输出环境的可执行文件
		- gcc -fexec-charset = gbk  在编译过程，生成 gbk 可执行文件
	- 运行时环境：
		- Windows：默认使用 GBK
		- Linux：  默认使用 utf

- 常用标点符号

	- ~：波浪线  按位取反
	- `：反撇号   C 没有定义该符号  shell 编程中有用
	- !：逻辑取反（非真即假）
	- @：C 没有定义该符号
	- #：预处理前缀符号
	- $：C 没有定义该符号
	- %：取模
	- ^：2 个操作数按位异或
	- &：2 个操作数按位想与
	- &&：2 个表达式 逻辑与
	- *：2 个操作数相乘    指针定义符号   指针取用符
	- ()：优先级符号  函数的输入参数定义  函数调用
	- -：2 个操作数相减    负数
	- _：C 没有定义该符号
	- +：2 个操作数相加
	- =：赋值（按位拷贝）
	- ==：逻辑相等
	- [ ]：数组下标    空间访问符
	- {}：函数体 结构体的成员列表  数组空间的初始化     一块空间
	- |：2 个操作数 按位或
	- ||：2 个表达式 逻辑或
	- \  : 转义符（改变原本的意义）   续行符  (\  后面跟的数字是八进制)
	- :  ：标签的标识（不占内存空间）    case 10： printf(“123”);  // 只是一个指令， case 10 是标签
	- ;   ：语义分隔符
	- “ ：字符串常量 （连续空间，所有连续空间、必须增加结束标志  `\0` ）
	- ‘ ：一个字符（1B）
	- < ：逻辑小于
	- << ：位运算 左移
	- , ：逗号表达式  int a  = 10，20，30；（a = 30 不常用）  多用于配合{}初始化
	- \> ：逻辑大于
	- \>\> ：位运算 右移
	- . ：结构体里成员访问，结构体变量访问成员
	- -> ：结构体里成员访问，结构体指针访问成员
	- ? ：  `? :`  三目表达式
	- /  ：2 个操作数 相除

- 运算标识： + -  *  /  （注意越界问题 —— 溢出      数据类型容量）

	- `+ -`：翻译成指令，cpu 自带的指令  负数会变成补码从而变成加法  硬件电路之间实现
	- `* /`：部分 cpu 没有乘法器、除法器这些硬件电路
		- 解决方式：软件模拟  软件乘法、软件除法 （用 + - 循环 移位 模拟 * /）
	- ++ ：a++（先用后加  ——  先把 a 当作表达式的结果，返回这个表达式结果，再改变 a） ++a（先加后用 —— 先改变 a，再当成表达式的结果，返回这个表达式的结果）

- ^ 异或

	- 用于场景

		- 加密、解密

		- 交换两个数、不用第三个变量

			- ```c
				a = a ^ b;
				b = a ^ b;
				a = a ^ b;
				```

			- 

- %：得到循环数 限制范围   %n （0  ~ （n - 1））





## 2026-5-19

- 常用运算

	- 算术运算

	- 位运算

		- &      按位与     屏蔽某些位，提取某些位，某些位设置为 0

			- ```c
				 /* 提取低3位 */
				int a = 10101010; 
				
				a &= 0x7;  
				```

		- |  按位或      对某些位设置为 1，其他位不受影响

		- `>>`:  

		- `<<` :

		- 芯片  由引脚构成（1 根引脚  = 1 位）  程序 通过 寄存器（某一位 依附于 一个寄存器 —— 大小不小于 1B） 控制引脚

		- 某个引脚置为 1

			- ```c
				/* 对a变量的5bit的位设置为1 */
				int a;
				a |= (0x1 << 5);
				```

			- 

		- 某个引脚置为 0

			- ```c
				/* 对a变量的5bit的位设置为0 */
				int a;
				/* a &= 0xFFDF; // 可移植性较差 */
				a &= ~(0x1 << 5);
				```

			- 

		- 某几个连续的引脚设置为 某个特定的值

			- ```c
				/* 对[8:4]位设置成 10110 其他位不变 */
				int a;
				a &= ~(0x1F << 4); /* 先清零 */
				a |= (0x16 << 4);  /* 在置1 [8:4]已经全是0的情况下 */
				```

		- 多组连续的引脚设置为特定值

			- ```c
				/* 对[10：8]置为100 ,[4:3]置为10 */
				int data;
				
				data &= ~(0x7 << 8 | 0x3 << 3);  /* 清零 */
				data |= (0x4 << 8 | 0x2 << 3);
				```

		- 设备中收到了一个 int 大小的数据 data，判断第 6bit 是否为 1

			- ```c
				int data;
				/* 数据的第6位是否为1 */
				/* 不能写成 data & (0x1 << 6) == 1 */
				if(data & (0x1 << 6)){
				    /* 处理代码 */
				}
				
				/* 数据的第6位是否为0 */
				if(!(data & (0x1 << 6))){
				    /* 处理代码 */
				}
				```

			- 

	- 逻辑运算（非 0 为真，0 为假    ）

		- 0 0x0  ‘\0’ 都能表示假

		- &&   有先后关系的执行      A && B 不能与 B && A 等价

			- ```c
				if(A && B){}
				
				/* 等价于 */
				
				if(A){
				    if(B){}
				}
				```

			- 

		- ||  预防性编程

- 关键字 

	- if 没有加括号 只能影响一行

	- switch 分支条件只能为整型

		- 必须使用整型的地方
			- %
			- switch

	- for：中 i++会备份一份旧的状态，同时最后会返回旧状态（比 ++i 耗时）  次数有关

	- while ：条件有关

	- ```c
		// 面试问
		a+++a++;
		
		// a + (++a)++
		// a++ + a++
		
		// 不回答a等于什么;
		// 回答：不同编译器不同结果，（有歧义）实际开发不推荐这样写
		// 一般问这个就寄了，可以和面试官聊其他的
		```

- 遍历法 

	- 从头到尾依次处理 （确定区间，区间的映射范围是计算机能表示）
	- 循环，每次都要做什么事务，每次处理的元素（操作对象），都要做同样的事情
	- 寻找退出的条件，满足条件，退出
		- 退出的可能性，拆分处理 =》 循环终止的位置，考虑结果的可能性（循环次数完 、break 退出）

- 循环：先循环体（循环规则），最后写循环退出条件






## 2026-5-20

- 指针思想

- CPU 执行指令     操作码+操作数

	- CPU 里面存储的时经常使用的操作码和操作数，且掉电丢失
	- 大部分的操作码和操作数存在再主存或者硬盘上

- C 语言如何分配、访问内存（2 种）

	- 内存：分配空间(容量大小)、定义空间的行为（有、无符号）     多个对象，每个对象的大小限制（上下偏移 =》 数组、指针）   

	- 存储体系结构体

		- 访问速度  寄存器  >>  内存 >> 外存  >> 网络

		- 寻址方式  编号            地址          块号（扇区号、柱面号）

		- 容量           少                 居中                最大 

		- 体系结构：冯诺依  曼结构、哈佛结构（流水线结构  ）  

			- 现代计算机是宏观冯诺依曼（CPU + 内存 + 硬盘 + 外设），微观是哈佛体系（CPU 里面有专门的指令 Cache，数据 Cache）

		- 地址线：有多少个单元，数据线：每个单元的多大

		- 控制线：控制读/写、片选 

		- 32 位/64 位的系统，一个字（word） == 4 个字节

		- 16 位系统，一个字 == 2 个字节

		- 芯片容量 = 地址线(有多少个) * 数据线（每个多大）  如： 4G × 16  地址线有 32 根（2^32^GB），数据线有 16 根

		- >“某 CPU 有 16 根地址线（A0-A15），8 根数据线（D0-D7），读写控制线为 WE‾*W* *E*。现有容量为 **4K × 4 位** 的 RAM 芯片。
			>
			>1. 需要多少片这样的芯片？
			>
			>2. 画出 CPU 与这些芯片的连线图（或问：CPU 总共需要引出多少根线连接到存储器？）”
			>
			>	**总连线数 = 地址线根数 + 数据线根数 + 控制线根数**

			- 主存中的缓存区指的是 页缓存

			- 缓存 和 缓冲的概念：

			- | 维度         | **缓冲区（Buffer）**                       | **缓存（Cache）**                        |
				| ------------ | ------------------------------------------ | ---------------------------------------- |
				| **主要目的** | **速度匹配**：协调不同速度的设备/组件      | **加快访问**：减少重复读取相同数据的开销 |
				| **典型场景** | I/O 操作（磁盘、网卡、打印机等），主存中的 | CPU 访存、磁盘读写、网页、数据库等        |
				| **数据特征** | 数据通常 **只用一次**（用完就丢）           | 数据 **可能被反复使用**（热点数据）       |
				| **管理方式** | 一般简单（先进先出、循环缓冲、双缓冲）     | 复杂策略（LRU、LFU、FIFO 等替换算法）    |
				| **是否命中** | 一般不需要“命中”概念                       | 有 **命中率（Hit Rate）** 概念             |
				| **例子**     | 打印机缓冲区、视频播放缓冲、I/O 缓冲区      | CPU L1/L2/L3 缓存、浏览器缓存、Redis 缓存  |

			- 里 cpu 越远，容量越大，速度越慢（在使用的线有限的情况下，通过多个时间周期来获取数据或者发送信号）

	- 指令 操作数 存在哪里

	- ```c
		cpu（本身是按找一个字节处理）发出
		C程序有int a；
		假如a的地址刚好是0x100，那么cpu就希望0x100~0x103都是a的内存空间
		那么发送到SDRAM取值时，就应该以4个字节偏移，所有需要舍弃LADDR0和
		LADDR1，而一片SDARM里面接收到的地址是0x100 >> 2 = 0x40，从这个地址上读2个字节，两个SDARM的结果组合成4B
		0x100  
		0x101
		0x102
		0x103
		
		cpu只想在发出0x104时，SDRAM才返回4B
		0x104  
		    
		    
		如果想要cpu想要读取char类型，SDRAM依然返回4B，只是cpu只读最低一位的字节，存入cpu
		如果想要cpu读取long long型，会发送两次地址，SDRAM返回两次结果
		```

	- 

- CPU 语言（）

	- 按照字节为单位

- C 语言编译器 高级语言  =》 关注对象 

  - 映射表（记忆表）
  	- 普通变量名   某个地址   容量大小
  	- 指针变量名    某个地址   容量大小、后续访问的下一个对象是多大 ？
  - 存储 二进制 的信息   基本数据类型
  - 存储 地址 的信息  
  	- 地址的容量
  		- 编译器来决定  gcc -v 中的 target 查看（64bit） 8B   （32bit）4B
  	- 地址可以访问，如何访问内存（下一个对象是多大）

- 普通变量 分配一个 保存数据类型大小的容器

	- 运算    = 》 数学规则  支持位、逻辑、算术、运算

- 指针 分配一个  保存能够存储地址信息大小的容器 ，他是一个特殊结构 （该存储地址 是否有效）

	- 二义性（两个意义）  编译器必须在编译阶段得到 2 个问题得到答案, 才能工作

		- 容器大小
		- 容器的间接访问内存的行为

	- 运算  =》 如何访问下一个对象 =》± 1  ：地址向上/向下一个对象（单位）大小的偏移     对象的大小，存的地址的类型大小(对象的大小)，其他运算没有意义

	- ```c
		int *a;
		
			// 以变量名(编译器不认识的值)为中心，先右后左，依次回答问题
			// a 遇到* 升级成指针变量，容量为8B，如何访问对象？
			// 以*a为中心，先右后左，依次回答问题
			// *a遇到 int , 访问的对象是4B大小
		
		int a1[5];
		int *a2[5];
		int (*a3)[5];
		int (*f)(void);
		int *f(void);
		```

- 函数地址，不支持运算（或者 没有意义，不同编译器有不同描述行为）

- 类型转换

- 无限访问 int a [10]; a [100]; 能访问，但不合法

- p [-100] == *（p - 100）  能访问，但不合法

	- 没有打印，原因是操作非法空间，操作系统会杀死
	- C 语言对指针的访问，不做越界检查，执行时，被操作系统杀死

- 取值： * (类型提升符)     完成通过指针里保存的值，间接的找到其他地址的里面内容

- 取地址：&  不能大端还是小端，取到的地址都是低位地址（首地址）

- *p == p [0]   *(p + 100) == p [100];

- 声明有数据类型，没有数据类型就是使用

	



- 数组

- 本身是一种数据结构（有限的线性表），不是数据类型，所有不会管越界

- 语法形式

- arr1 数组名 不是 变量 只是 **连续空间** 的标签  不占用内存空间     常量(编译时就知道了)   只有 sizeof 时才当成整个连续空间，其他时间就是常量（连续空间的首地址的标签）

- 等长元素构成的表

- 所有数组名 不能被赋值

- 数组的使用 

	- 一旦定义后，再使用时，思路就是 **逐个处理**   其余空间值为随机值 （待验证）

	- 定义前，可以使用语法糖简化编写 {}   编译时就已经处理好了  部分初始化，其余未初始化的赋值为 0（推荐）

	- ```c
		char data[] = "hello";  
		
		// 本身不合理 数组 != 地址
		// C语言内部，会自动调用strcpy函数（逐个拷贝）实现data空间的初始化
		// 裸机开发时，会出现编译不通过，原因有些裸机缺少strcpy函数
		```

	- 

- ”” ：字符串常量 本质就是一个地址（首地址） 标签

- 指针变量 = 数组名；

	- ```c
		int arr1[5];   // arr1 arr1+ 1
		int *p1 = arr1;
		
		int arr2[4][3];  // arr2 arr2 + 1  +1行
		int (*p2)[3] = arr2; 
		```

- %s：以后面的变量值 做为首地址，按照字符解码方式，逐个解码，遇到‘\0’ (0x0 值)终止

- 小数标准：lEEE745



## 2026-5-21

- 二进制空间（连续）

  - 访问空间时，需要防止越界，方法（规定结束标志 或者 规定范围）
  - 数据空间 数字空间
  	- 数据：无符号、采集数据
  	- 数字：有正负含义 能数学运算
  	- 区分：定义时 使用 unsigned 即为数据
  - 字符空间 非字符空间

- 字符空间

  - 区分

    - 字符空间
    	- C 语言：多个字节的空间里，如果把 0 值当作结束标志 的空间，称字符空间（可读可写） ，字符串（只读）
    	- 最小单位 1B
    	- 存放字符串
    - 非字符空间（0 值有意义）
    	- 约束该空间的个数
    	- 存放二进制数据

  - 如何描述空间

  	1. 指向一个 int 空间的首地址      int a;  int *p = &a;
  	2. 指向一片 int 的数组空间（一片连续空间）  —— 使用两个变量共同描述一片空间             int arr [5];    int *p = arr; int num = 5;    (p 和 num 共同描述这片空间  这个两个可以封装称结构体)
  	3. char *p;
  		- 指向一个 char 空间的首地址（很少用，一个 char a 就能描述）
  		- 指向一片 char 的数组空间
  			- 约定了 0 值为结束标志 —— 字符空间
  			- 有个数 —— 非字符空间
  		- 问题：
  			- 有些编译器认为 unsigned char 和 char  一样 
  			- 后续 描述一个传感器 采集的 数据集  数据空间（1B 为一个采集单元）
  			- uint8_t *p;  int num; 替代 unsigned char * p; int num   可读性高 
  			- char *p  描述一个字符空间

  - const 关键字  

  	- 建议不能改变    告诉程序员，这个变量是只读，代码上建议不能修改（可以越界去修改）

  	- 去数据类型，看 const 修饰哪个空间  

  	- ```c
  		const char *p1;
  		char * const p2;
  		const char * const p3;
  		char const * p4;
  		```

  - 初始化字符空间

  	- ```c
  		// 1. 字符数组，申请空间
  		char arr1[] = "hello"; // 使用字符串常量进行初始化，使用strcpy() 逐个拷贝到arr1空间中，0值也会被放入到arr1中
  		...
  		char arr2[] = {"hello"};
  		 // 非字符空间初始化，建议使用大括号
  		char arr3[] = {'h','e','l','l','o'};
  		// 逐个赋值 arr3中没有存储0值，一旦通过%s、strlen（字符串相关）使用，会出现随机结果
  		
  		
  		
  		// 使用指针 指向空间
  		
  		// 多个相同常量，在可执行程序只用一份，其余的相同常量只是一份地址信息
  		
  		// sizeof只是将创建空间时的大小，复制过来
  		```

  - 字符空间的访问行为

  	- 遍历 while
  	- 法 1：
  		- 两个相同类型的指针相减 == 对象的个数（不是字节数）
  		- [x, y]  => y - x => 没有计算 y   比实际少统计一个
  		- [x, y) => y - x 和实际个数相同

  - 标志 C 库提供的常用字符串处理函数

  	- ```c
  		// 统计字符空间的个数, 传统任意空间，内部只关心0值为结束
  		strlen();
  		
  		// 拷贝字符串  不太推荐使用，会导致越界修改(危险函数 —— 扣钱函数)
  		strcpy();
  		
  		// 拷贝字符串 推荐使用 双保险 不一定会放入0值标志  后续位置需要留0值位置，所以需要 - 1个长度 n:Number
  		strncpy();
  		
  		// 内存拷贝，能拷贝0值 字节拷贝
  		memcpy(); 
  		// 内存区域设置某值 不能做到连续空间中每个对象设置成相同数（int 4B） 常用于清零  字节设置
  		memset();
  		
  		// 格式化生成字符串的接口函数   本质是将数据打印到内存中 会将0值一起输出进去 (不能使用)
  		sprintf();
  		// 高频使用 会自动拷贝最多 n - 1个字符，内部一定有0值结束标志
  		snprintf();
  		
  		```

  - `...`：不定参数（可变参数列表）

  - 数组的概念，在编译时，编译器认可，编译时把所有非运行时结果，在编译器就计算

  	- 一旦开始运行了，就没有数组概念，所有数组名，在编译时，不确定答案，数组名 == 首地址传递

  - _t ：已经被 typedef 定义，让人能知道数据的实际含义

  	- uint8_t：
  	- size_t： 内存字节单位 B

  - ```c
  	strstr(); // 从一个字符空间 找 另一个字符空间一样的内容  长串中找短串
  	
  	
  	
  	```

  - 

- 多维数组、多维指针

  - 多维指针  最多二维指针
    - 本质：实际内存上不连续的多个离散空间，逻辑上变成线性结构
    - 二维指针 指向一个一维数组空间，只是这个数组里的每个元素，是另外一个空间的首地址
      - main 函数的参数列表

- 内存越界：超过限定区域





## 2026-5-22

- 二维数组（空间连续）

	- 二维数组的定义，告诉编译器如何访问内存的索引计算方式

	- 数组可以认为是指针的语法糖，在运行时已经转换指针

	- 一位内存 映射成 行列的二维

		- 目的为了程序员之间阅读代码的便捷性 坐标系计算集合、线代

	- 定义一个指针变量，来保存二维数组、三维数组名

		- `int arr1[m][n]; int (* p)[n] = arr1;`
		- `int arr2[x][m][n];int (*p)[m][n] = arr2;`

	- 开发还是 关注逻辑坐标， 具体逻辑空间转 内存空间，编译器来进行决定

	- 多维空间的 逻辑访问方式

		- 遍历  按照逻辑级别处理（多层循环）

	- 二维数组访问方式的时间

		- 前提（cpu体系架构有关）：缓存（Cache）访问主存时，会将一片连续时间的空间拷到Cache中，Cache空间有限
		- 按行访问，内存会连续访问，Cache一次访问内存，能读出更多有效数据
		- 按列访问，内存访问不连续，访问时间更久

	- ```c
		clock(); // 记录当前的计算机的节拍数 单位次数
		```

- 二维指针（优化switch分支 、方便维护、将离散的空间，统一放入连续空间）

	- 实际指向一维空间（内存上），这个空间里，每个元素项，保存了指针类型
		- 一个连续钥匙串，每个环上挂一把钥匙
		- 物理上不连续的空间，逻辑上要求连续
	- 小测试：一周做事，可变

- 内存分段管理（大概念上，有问题就加层）

	- strlen() - 1 后期项目回车符替换成0值

	- 操作系统（OS）为了保证系统的运行稳定性，每个程序运行时的环境（内存空间）进行分段管理

		- 程序 访问的资源 CPU能够寻址到的空间
		- 操作系统提供虚拟地址（统一地址） ，然后将虚拟地址 映射成物理地址（物理空间不统一）
		- 虚拟地址的大小 == OS的位数有关
			- 物理内存 不能超过  虚拟内存
			- 64位的操作系统 真实寻址能力为48位

	- 4G虚拟空间为例 分段  （现代操作系统是分页）

		- 段错误是操作系统报的错误

		- 内存分段

			- 内核空间（操作系统） 应用程序R、W都报错
			- 栈段 局部变量  编译器来维护生命周期
			- 堆段  动态申请的空间  程序员来维护生命周期
			- 动态库段
			- 数据段(.data段)  —— 生命周期是最长的 程序运行 —— 程序退出
				- .rodata   常量  只读
				- .data        全局变量、静态变量   
				- .bss   未初始化全局变量、静态变量（后续会利用0进行占位）
			- 代码段（.text段）    所有的函数（指令 ）—— 按指令解析    只读（反汇编查看）
			- 从0x00~代码段有一小部分空间 操作系统管理 程序RW报错

		- 程序的编译、链接后的可执行文件大小 = 代码段 + 数据段（静态段）

		- 定义任何空间时，可以指定该空间在链接时，分配到哪个段里（通常使用默认分段）

		- 堆区 和 栈区

			- | 管理者/管理区域 | 操作系统 | 编译器 | 程序员 |
				| --------------- | -------- | ------ | ------ |
				| .text           | 管理     |        |        |
				| .data           | 管理     |        |        |
				| 栈              |          | 管理   |        |
				| 堆              |          |        | 管理   |

			- 栈区：

				- 没办法达到 动态申请

				- 根据声明情况（编译期），调整 栈指针 实现申请空间， 函数返回时，调整栈指针 释放空间（数据还在）

				- 从上向下一次放入，效率高

				- ```c++
					int a = 1;
					char arr[a]; // 本质是向堆区申请空间
					```

				- 

			- 堆区：

				- 运行时，向操作系统申请 可变空间  操作系统维护 哪些空间空闲
				- 向操作系统注销
				- 效率低（用户态 与 内核态切换）
				- 操作系统 申请了一个空闲空间，返回空间的首地址 （匿名空间） —— malloc（不同操作系统 调用的接口不同） —— 用一个指针接收
				- 内存泄漏：匿名空间存在，但找不到了（没有释放）   程序继续运行，系统内存会被占满 
				- double free：多次释放堆区，程序会被OS杀死 图

- 内存分段管理

	- 分段

		- 管理时机：编译器 在链接时 确定 每个目标文件（所有的.c文件）在内存所在的位置
		- 约定内存有一个分段分区的标准  操作系统  =》 虚拟内存
		- 面向硬件编程 没有OS，但是编译器默认有 分段行为  每个段所在的基地址 结合 具体硬件 进行配置

	- 栈区

		- 所有函数的局部变量 =》 栈区
		- 进入函数 ——》 到函数返回
		- 栈区的大小限制 ：希望整个栈区能够为更多的子函数提供服务
			- 每个子函数的局部变量 有一个大小限制
			- 以后的交叉编译（PC上写代码 开发板 上 运行） PC上写的代码默认以PC为基础分配栈，移植到更小的开发板 ，导致闪退  =》栈溢出 =》所以局部变量尽量少
			- 递归函数 =》 容易引发栈溢出
			- Linux栈溢出现象（有提示）图

	- 堆区

		- 空间无名，返回首地址

		- 无法在开发时 确定空间大小，必须在运行时才能知道空间大小 =>使用堆区

		- 先通过某种算法，得到新科技大小

		- ```c
			xx *p = malloc(单位是字节 sizeof(每个元素的数据类型) * 个数);
			if(p == NULL) {...}
			free(p); // 不能重复释放
			p = NULL;	// 防止重复释放导致系统杀死 free(0 / NULL); 不做任何操作
			```

		- 生命周期 malloc ——> free

	- 数据区

		- 静态区（static 关键字）
			- 修饰的这个空间 分配到数据区（分配到生命周期很长的区域 —— 程序结束死亡）
			- 与全局变量 区别
				- 全局变量 所有子函数，都能访问（提升作用范围）
				- 静态变量 不会改变变量的作用范围，常驻内存，只初始化一次 （如果是赋值，则是多次）



## 2026-5-25

- 函数名的含义

	- 函数名 == 标签（常量地址 —— 编译器编译时临时使用，一旦生成了可执行文件，常量地址运行时不占用空间 —— 放入指令） 图

	- 数据名 == 标签

	- 函数指针：保证函数名的指针变量

		- `返回值 (*指针变量名)(参数列表)`

	- |                  | 函数指针(回调函数、钩子函数) | 普通指针 |
		| ---------------- | ---------------------------- | -------- |
		| 占用地址大小容量 | 4/8B                         | 4/8B     |
		| 运算             | ± 没有意义                   | ±1       |
		| 取内容           | (*func)() == func()          | *p       |

	-  函数指针使用场景

		- 把物理上离散的函数名，变成逻辑上连续的 —— 函数指针数组
		- 跨平台设计时，设计时用的（指向了不同驱动的具体实现函数，让应用层调用统一接口） —— 解耦（让各个模块独立）
		- 写的例子：不同身份客户，使用不同功能（VIP调用不同的函数，普通调用另一个函数）

	- 

- 函数的输入参数 —— 接收信息

	- 函数声明（定义）：告知编译器 这个名字表示什么 —— 函数使用前进行声明，主要在头文件中声明

	- 形成列表的内容，调用时申请了局部空间，由函数调用时，传递（赋值 —— 值传递、地址传递都是一样的拷贝）

	- 由函数的调用者，通过实参传递过去（拷贝，依然满足大到小 —— 截断，小到大—— 补齐）

	- 函数中，如何接收任何空间

		- 独立空间，一个数据类型

			- 基本数据类型的变量已经存在，通过 函数形参来申请
			- 结构体/共用体：是数据类型。支持拷贝 一般结构体都是多个成员组成，拷贝结构体的时机开销、空间开销大，一般传地址
			- 数组：不是数据类型，不能拷贝

		- 连续空间（必须规定结束标志）

			- 字符连续空间

				- 隐式标志：传递者把0值结束标志已经构造到空间里，再把这个空间的首地址传递

				- 字符的连续空间，是查看还是间接修改

					- ```c
						
						// 绝对不修改，只看，加const
						void fun1(const char *a){
						  while(*a){++a;}  
						}
						
						// 需要修改 不加const
						
						//printf
						// snprintf
						```

					- 

			- 非字符连续空间

				- 特殊空间

				- 没有结束标志，使用者没有约束，一般传递个数做为标志

				- 明确知道一个连续空间有多大

					- ```c
						void f3(struct stu *table, int num){
						    for()
						}
						```

					- 只读（const修饰），可修改

				- 例子：

					- ```c
						// 二维数组
						// 进行图像的按行均值化处理
						int blur(char (*image)[600], int row);
						```

					- 

				- 纯数据空间

					- 如何在函数形参中表示任意空间 —— void *（不能做为变量声明，不能做为实参  接收了一个任意空间的首地址）
						- 只能在形参中出现，收到任意空间首地址后，按照字节为单位进行逐个处理   声明时，还需要限制以字节为单位的个数
						- 会强转成char *
					- void f(void *, size_t num);
					- 只读（const）、可修改
					- 

				- 数字空间

					- 

- 函数的返回值 —— 反馈信息

	- 可以通过输入信息返回信息

	- 函数通过输入参数，来更新上层空间，条件

		1. 调用者必须 传递要更新空间的首地址

		2. 下层空间里，必须访问这个地址指向空间，放在等号左边

			- ```c
				void fun()
				```

			- 

	- 二维指针应用场景

		- 出现在函数形参时，可能表示更新一维指针空间
		- 出现在函数形参时，可能表示一个数组空间，只是这个空间里保存其他空间的首地址（让物理上不连续的空间，逻辑上变得连续）
			- 不仅仅有二维指针，而且还要一个数量的数据类型，一起配件使用
			- 有些会约定NULL（0值）为结束标志

	- strtoul => to转换 ul unsigned long

	- 所有函数返回值，逐位拷贝，一旦返回值是指针的，指向的空间，必须保证函数返回后，依然有效

		- 数据区：static、全局变量（不常用）、只读区（不常用）
		- 堆区
		- 区别在哪个区的方法
			- 堆区 —— 接口成对出现（有开、有关） 如：fopen(),fclose()
			- 数据区：只用一个接口

- 写游程编码



## 2026-5-26

- 多模块编程原理（多文件）
	- C语言编译器：
		- **每个源文件 单独编译成 目标文件**    每个函数的实现，最终就是一个函数名的符号表。
		- 多个目标文件 链接成 可执行文件
	- `nm .o文件`：查看二进制文件的符号表
		- `T` == `text` 全局？
		- `t` == `t` 局部
		- `U` == `undefine`
		- `__` == 标志库函数 ？
		- 链接时的装配器，只关注符号表（不会检查 函数的输入行为和返回值）
		- 符号表缺少了    报错 缺原材料
		- 符号表多了（重复）   报错 多次定义
		- 能运行，但是结果不定
	- 解决能运行，但是结果不定
	- 在使用前声明这个函数，让编译器在编译阶段报错 （**所有函数，编译器会自动生成 跳转指令，必须提前声明函数的形式，编译器有了评价依据**）
	- 函数的实现者，要维护接口的声明（如果使用者声明，会导致结果不一定） —— 引出头文件 —— 存放函数声明
	-  避免头文件重复引用 —— 防御性编程  —— guard header 宏
- 自己的定义的函数名与别人定义的重复了，自己的函数添加static，编译器会优先使用局部的函数 ？
- exturn ： 全局的
- static：提升生命周期，改变作用域
	- 修饰局部变量，变量分配从栈区到静态数据区，生命周期 从当函数变到整个程序
	- 修饰函数名，函数从全局有效变为本文件内有效
	- 修饰全局变量，将全局变量（任何模块都有效）变成 当前文件有效
- 常量不能取地址（执行时是没有意义的） 如果要取地址，他的含义被编译器重定义了
	- 数组名 数组空间的首地址
	- &数组名 编译器自行的行为，地址表示一个数组一个数组大小的访问 ，也就是看成二维数组



## 2026-5-27

- 讲题
